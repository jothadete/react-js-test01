import React from "react";
import "./style.css";

export default function App() {
  return (
    <div>
      <h1>Hello StackBlitz!</h1>
      <p>Start editing to see some more changing magic happen :)</p>
      <h2>And run it locally</h2>
      <p>First install npm into the project folder: npm install</p>
      <p>And then run (npm start) and/or test (npm test) it. </p>
      <p>Don´t forget to git-ignore the node_modules...</p>
    </div>
  );
}
